---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: applications.enbuild.vivsoft.io
spec:
  group: enbuild.vivsoft.io
  names:
    kind: Application
    listKind: ApplicationList
    plural: applications
    singular: application
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.owner
      name: owner
      type: string
    - jsonPath: .spec.repository
      name: repository
      type: string
    - jsonPath: .spec.infrastrcture
      name: infrastrcture
      type: string
    - jsonPath: .spec.infrastrcture
      name: logs
      type: string
    - jsonPath: .spec.traces
      name: traces
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Application is the Schema for the applications API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ApplicationSpec defines the desired state of Application
            properties:
              dashboard:
                description: Dashboard of the Application.
                type: string
              infrastrcture:
                description: Infrastrcture  of the Application.
                type: string
              logs:
                description: logs of the Application.
                type: string
              owner:
                description: Owner of the Application.
                type: string
              repository:
                description: Github Repository of the Application.
                type: string
              traces:
                description: traces of the Application.
                type: string
            required:
            - owner
            type: object
          status:
            description: ApplicationStatus defines the observed state of Application
            type: object
        type: object
        x-kubernetes-preserve-unknown-fields: true
    served: true
    storage: true
    subresources:
      status: {}
