---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: catalogs.enbuild.vivsoft.io
spec:
  group: enbuild.vivsoft.io
  names:
    kind: Catalog
    listKind: CatalogList
    plural: catalogs
    singular: catalog
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: type
      type: string
    - jsonPath: .spec.repository
      name: repository
      type: string
    - jsonPath: .spec.ref
      name: ref
      type: string
    - jsonPath: .spec.role
      name: role
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Catalog is the Schema for the catalogs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CatalogSpec defines the desired state of Catalog
            properties:
              components:
                description: components of the Catalog
                items:
                  properties:
                    image_path:
                      description: image_path of the Catalog.
                      type: string
                    name:
                      description: Role which can access the Catalog.
                      type: string
                    project_id:
                      description: Gitlab ProjectID  of the Catalog.
                      type: integer
                    ref:
                      description: ref of the Catalog.
                      type: string
                    repository:
                      description: Github Repository of the Catalog.
                      type: string
                    tool_type:
                      description: tool_type of the Component.
                      type: string
                    variable_file_path:
                      description: variable_file_path of the Catalog.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              description:
                description: description which can access the Catalog.
                type: string
              image_path:
                description: image_path of the Catalog.
                type: string
              readme_file_path:
                description: readme_file_path of the Catalog.
                type: string
              ref:
                description: ref of the Catalog.
                type: string
              repository:
                description: Github Repository of the Catalog.
                type: string
              role:
                description: Role which can access the Catalog.
                type: string
              secrets_folder_path:
                description: values_folder_path of the Catalog.
                type: string
              sops:
                description: ref of the Catalog.
                type: boolean
              type:
                description: Type of the Catalog.
                type: string
              values_folder_path:
                description: values_folder_path of the Catalog.
                type: string
            required:
            - components
            - role
            - type
            type: object
          status:
            description: CatalogStatus defines the observed state of Catalog
            type: object
        type: object
        x-kubernetes-preserve-unknown-fields: true
    served: true
    storage: true
    subresources:
      status: {}
